---
title: "03_graficado_de_datos"
format: html
editor: visual
---

## Ejercicio transversal 3 - Graficado de datos

### Cargamos las librerías "readr" y "dplyr"

```{r}
library(readr)
library(dplyr)
library(lubridate)
library(ggplot2)
```

### Descargamos los datos

Guardamos las URL en objetos y descargamos los archivos, de manera que el documento pueda correrse desde cero en cualquier computadora.

```{r}
estacion_0472_url <- "https://raw.githubusercontent.com/rse-r/intro-programacion/main/datos/NH0472.csv"
estacion_0910_url <- "https://raw.githubusercontent.com/rse-r/intro-programacion/main/datos/NH0910.csv"
estacion_0046_url <- "https://raw.githubusercontent.com/rse-r/intro-programacion/main/datos/NH0046.csv"
estacion_0098_url <- "https://raw.githubusercontent.com/rse-r/intro-programacion/main/datos/NH0098.csv"
estacion_0437_url <- "https://raw.githubusercontent.com/rse-r/intro-programacion/main/datos/NH0437.csv"
```

```{r}
dir.create("datos", showWarnings = FALSE)

download.file(estacion_0472_url, "datos/estacion_0472.csv")
download.file(estacion_0910_url, "datos/estacion_0910.csv")
download.file(estacion_0046_url, "datos/estacion_0046.csv")
download.file(estacion_0098_url, "datos/estacion_0098.csv")
download.file(estacion_0437_url, "datos/estacion_0437.csv")
```

### Leemos los datos con read_csv()

```{r}
estacion_0472 <- read_csv("datos/estacion_0472.csv")
estacion_0910 <- read_csv("datos/estacion_0910.csv")
estacion_0046 <- read_csv("datos/estacion_0046.csv")
estacion_0098 <- read_csv("datos/estacion_0098.csv")
estacion_0437 <- read_csv("datos/estacion_0437.csv")
```

### Unimos las estaciones en un solo data.frame con la función rbind()

```{r}
estaciones <- rbind(estacion_0472, estacion_0910, estacion_0046,
                    estacion_0098, estacion_0437)

glimpse(estaciones)
```

Cada tabla conserva la columna "id" con el código de la estación, así que podemos agrupar, con group_by() por esa variable sin perder de dónde viene cada fila.

### Ejercicios

1.  Graficá la temperatura de abrico para la estación NH0437 (tendrías que filtrar los datos para quedarte solo con esta estación). Sumá una regresión linea, ¿se mentiene constante la temperatura media con el tiempo o cambia?

```{r}
estaciones |>
  filter(id == "NH0437") |>
  ggplot(aes( x = fecha, y = temperatura_abrigo_150cm)) +
  geom_point(alpha = 0.1) +
  geom_smooth(method = "lm") +
  labs(
    title = "Temperatura media diaria en la estación NH0437",
    x = "Fecha",
    y = "Temperatura de abrigo a 150 cm (°C)"
  )
```

##### Conclusión:

Primero filtramos para quedarnos únicamente con la estación NH0437 y después graficamos la temperatura en función de la fecha. El argumento "alpha = 0.1" hace los puntos casi transparentes: son casi 19.000 observaciones y de otra manera se verían como una mancha sólida.

Respecto a la pregunta: ¿Se mantiene constante la temperatura media con el tiempo o cambia?

No se mantiene constante. La recta de regresión sube, pero muy poco: pasa de unos 19,7 °C a principios de los 70 a poco más de 20,5 °C en 2023. O sea, menos de un grado en más de 50 años. Igual, mirando el gráfico, ese aumento casi no se nota, porque las temperaturas van de 5 a 35 °C según la época del año. La variación entre verano e invierno es mucho más grande que el cambio de todas estas décadas juntas.

2.  Calculá el promedio mensual de la temperatura de abrigo para cada estación y gráfica el resultado. El gráfico tendría que tener más o menos esta pinta:

```{r}
estaciones |>
  mutate(mes = month(fecha)) |>
  group_by(id, mes) |>
  summarise(
    temperatura_media = mean(temperatura_abrigo_150cm, na.rm = TRUE)) |>
  ggplot(aes(x = mes, y = temperatura_media, color = id)) + 
  geom_line() +
  labs(
    title = "Temperatura media mensual en cinco estaciones del INTA",
    x = "Mes",
    y = "Temperatura media (°C)",
    color = "Estación"
  )
```

Conclusión:

Las cinco estaciones siguen el mismo patrón: temperaturas altas en enero y diciembre, y el mínimo entre junio y julio. Es el ciclo estacional del hemisferio sur.

Lo que cambia es el nivel. NH0437 está siempre por encima del resto (debe ser la más al norte) y se despega sobre todo en invierno, donde llega a estar casi 4 °C arriba de NH0046. En verano las cinco se juntan bastante más.

3.  ¿Cuál es la relación entre la temperatura de abrigo y la humedad? Hacé un gráfico de puntos que muestre la relación, podés sumar un suavisado (opcional). Para la humedad utilizá la varable `humedad_media_8_14_20`.

```{r}
estaciones |>
  ggplot(aes(x = humedad_media_8_14_20, y = temperatura_abrigo_150cm)) +
  geom_point(alpha = 0.1) +
  geom_smooth() +
  labs(
    title = "Relación entre humedad y temperatura",
    x = "Humedad media (%)",
    y = "Temperatura de abrigo a 150 cm (°C)"
  )
```

Conclusión:

La relación es negativa, a más humedad, temperaturas un poco más bajas. El suavizado baja de 25 a 17 °C aproximadamente entre los extremos de humedad.

Pero es una relación bastante débil. La nube de puntos es enorme y para una humedad de 75, por ejemplo, hay días de 5 °C y días de 30. Con la humedad sola no se puede predecir la temperatura.

Se quitaron 19.862 filas por valores faltantes, casi todas de NH0910, que no tiene ningún dato de humedad.

```{r}
estaciones |> 
  ggplot(aes(x = humedad_media_8_14_20, y = temperatura_abrigo_150cm)) +
  geom_point(alpha = 0.1) +
  geom_smooth() +
  facet_wrap(~id) +
  labs(
    title = "Relación entre humedad y temperatura, por estación",
    x = "Humedad media (%)",
    y = "Temperatura de abrigo a 150 cm (°C)"
  )
```

conclusión:

Separé por estación con `facet_wrap()` para ver si todas se comportan igual o si el promedio del gráfico anterior estaba tapando diferencias entre ellas.

Las cuatro que tienen datos muestran tendencia negativa, así que la relación se sostiene y no era un efecto de mezclarlas. Pero la fuerza cambia bastante: en NH0098 la caída es marcada (de 29 a 16 °C), mientras que en NH0437 y NH0472 la curva es casi plana. O sea que la humedad pesa más en unas estaciones que en otras.

El panel de NH0910 queda vacío, que es justamente la estación sin registros de humedad: acá se ve directo el faltante que antes solo aparecía como un warning.
